[repositories]
  [repositories.knoxitetest]
    url = "/tmp/knoxitetest"
    compression = "gzip"
    tolerance = 0
    encryption = "aes"
    pedantic = true
    verify_store = true
    store_excludes = ["just", "an", "example"]
    restore_excludes = ["just", "an", "example"]
